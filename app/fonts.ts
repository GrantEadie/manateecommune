import { Mulish, Space_Mono, Overpass_Mono } from "next/font/google";

export const inter = Mulish({ subsets: ["latin"] });
export const space = Space_Mono({
  subsets: ["latin"],
  weight: ["400", "700"],
});
export const overpass = Overpass_Mono({
  subsets: ["latin"],
  weight: ["400", "700"],
});